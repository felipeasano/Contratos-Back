package com.example.contrato.repositories;

import com.example.contrato.model.RepEmpContratada;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepEmpContratadaRepository extends JpaRepository<RepEmpContratada, Long> {
}
