package com.example.contrato.repositories;

import com.example.contrato.model.GesEmpContratante;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GesEmpContratanteRepository extends JpaRepository<GesEmpContratante, Long> {
}
