package com.example.contrato.repositories;

import com.example.contrato.model.EmpresaContratada;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EmpresaContratadaRepository extends JpaRepository<EmpresaContratada, Long> {
}
